const path = require('path');

module.exports = {
    DIST: path.resolve(__dirname, '../dist'),
    SRC: path.resolve(__dirname, '../src'),
    CONCIERGE_SRC: path.resolve(__dirname, '../src/concierge'),
    TERMINAL_SRC: path.resolve(__dirname, '../src/terminal'),
    SHARED_SRC: path.resolve(__dirname, '../src/shared'),
};